{
  "name": "fcbsousa",
  "version": "1.0.0",
  "description": "Brother's Blog",
  "author": "mlrcbsousa",
  "scripts": {
    "dev": "vitepress dev src",
    "build": "vitepress build src",
    "clear": "rm -rf src/.vitepress/dist",
    "serve": "vitepress serve src --port 8080",
    "start": "npm run clear && npm run build && npm run serve"
  },
  "devDependencies": {
    "@types/node": "^17.0.42",
    "vitepress": "^1.0.0-alpha.1"
  }
}
